# Spring Boot application properties
# Database Configuration - Using MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/medicnote_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=MedicNote@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# H2 Configuration (commented out - for development only)
# spring.datasource.url=jdbc:h2:mem:medicnote
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=create-drop

server.port=8081
server.servlet.context-path=/

# H2 Console Configuration (disabled for MySQL)
spring.h2.console.enabled=false
spring.h2.console.path=/h2-console

# JWT Configuration
jwt.secret=medicnoteSecretKeyForJWTTokenGenerationAndValidation2024SecureKeyForProduction
jwt.expiration=86400000

# Logging Configuration
logging.level.com.medicnote.backend=DEBUG
logging.level.org.springframework.security=DEBUG

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Validation Configuration
spring.validation.enabled=true
